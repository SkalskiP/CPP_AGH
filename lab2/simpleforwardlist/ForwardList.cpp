#include "ForwardList.h"

typedef struct ForwardList {
    struct node * head;
};

struct node {
    int val;
    struct node * next;
};

ForwardList *CreateNode(int value) {
    if (ForwardList.head == NULL) {
        struct node * new_node;
        new_node = new struct node
    }
};
